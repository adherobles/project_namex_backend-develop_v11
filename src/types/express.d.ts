// src/types/express.d.ts
import * as express from 'express';
/*
declare global {
  namespace Express {
    interface Request {
      // Aquí defines la forma de los datos que guardas en el token
      user?: {
        id: string;
        role: string;
        email: string;
      };
    }
  }
}*/